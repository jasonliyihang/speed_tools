<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/SpeedTheme">

        <meta-data
            android:name="root_class"
            android:value="com.example.clientdome2.OtherClass" />

        <activity
            android:name=".OtherMainActivity"
            android:exported="true"
            android:theme="@style/SpeedTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="jason.com"
                    android:path="/find_class"
                    android:scheme="speed_tools" />
            </intent-filter>
        </activity>
    </application>

</manifest>
